export const authRoutes = ["/Auth"]; // Pages d'authentification
export const apiAuthPrefix = "/api/auth"; // Préfixe des routes API d'authentification
export const DEFAULT_LOGIN_REDIRECT = "/accueil"; // Page après connexion
